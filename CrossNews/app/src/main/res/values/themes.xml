<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.NewsBiasTracker" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Window background matches the Compose app theme (newsprint light / warm dark). -->
        <item name="android:windowBackground">@color/news_background</item>
        <item name="android:colorBackground">@color/news_background</item>
    </style>
</resources>